# @cloudflare/workers-types se actualiza casi a diario; sin esto, pnpm 10+
# bloquea la instalación cada vez que la versión resuelta se publicó hace
# menos de 24hs (política anti supply-chain-attack, no es un error nuestro).
minimum-release-age=0
